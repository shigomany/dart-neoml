import 'package:dart_neoml/dart_neoml.dart';

void main() {
  final rnd = Random();
  print(rnd.normal(mean: 1, sigma: 4));
  // SIGMOID в C++ передается хуево
  // final cls = LinearClassifier();
  // print(ss2.sigmoid);
  // print(ss.desc);
  // final rnd = Random();
}
